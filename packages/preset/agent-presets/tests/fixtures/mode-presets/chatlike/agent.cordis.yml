# /mode test fixture preset: one named tool, so a switch is observable in the
# agent's model-facing catalog.
- id: chatlike-tool
  name: ../../plugins/contribute.js
  config:
    tool: chatlike_tool
