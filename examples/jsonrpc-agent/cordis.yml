# Unattended coding-agent deployment for the bundled dsh-jsonrpc-agent runtime.
# stdout is reserved for JSON-RPC; do not add a console logger or terminal UI.

- id: sdk-jsonrpc-server
  name: '@open-harness/oh-sdk-jsonrpc-server'
  config:
    maxTokensAsSuccess: !!js "process.env.OH_MAX_TOKENS_AS_SUCCESS === undefined ? true : JSON.parse(process.env.OH_MAX_TOKENS_AS_SUCCESS)"

# The DeepSeek adapter. Shipped default: full thinking at max effort on every
# request; exact-model resolution materializes request defaults before logging.
# The model arrives per session over JSON-RPC, so it is not pinned here.
- id: llm-deepseek
  name: '@open-harness/oh-llm-deepseek'
  config:
    thinking: enabled
    reasoningEffort: max

# Managed child-process groups for the bash executor (spawn/kill/output plumbing).
- id: subprocess
  name: '@open-harness/oh-subprocess-local'

- id: bash
  name: '@open-harness/oh-bash-local'
  config:
    cwd: !!js process.env.OH_CWD ?? process.cwd()
    timeoutMs: 60000

- id: agent-spine
  name: '@open-harness/oh-agent-spine-demo'
  config:
    persona: !!js process.env.OH_SYSTEM_PROMPT ?? 'You are a coding agent.'
    workspaceContext: false
    skills:
      enabled: false
    toolBash:
      enableRunInBackground: false
    toolJobs: false

- id: sessions
  name: '@open-harness/oh-session-persistence-jsonl'
  config:
    root: !!js process.env.OH_SESSION_ROOT ?? './.sessions'
    # Snapshot runs read the raw JSONL back; production keeps zstd frames.
    compression: !!js "process.env.OH_SNAPSHOT === undefined ? 'zstd' : 'none'"

- id: session-checkpoints
  name: '@open-harness/oh-session-checkpoint-policy'

- id: subagent
  name: '@open-harness/oh-subagent'

- id: subagent-spawn-in-process
  name: '@open-harness/oh-subagent-spawn-in-process'
  config:
    providerName: spawn

- id: tool-subagent
  name: '@open-harness/oh-tool-subagent'
  config:
    provider: spawn
    toolName: subagent
    enableRunInBackground: false

- id: tool-todo
  name: '@open-harness/oh-tool-todo'
  config:
    allowParallelInProgress: true

- id: fs-local
  name: '@open-harness/oh-fs-local'
  config:
    cwd: !!js process.env.OH_CWD ?? process.cwd()

- id: fs-observation-policy
  name: '@open-harness/oh-fs-observation-policy'

- id: tool-fs
  name: '@open-harness/oh-tool-fs'

- id: token-meter
  name: '@open-harness/oh-token-meter'

- id: compaction-basic
  name: '@open-harness/oh-compaction-basic'
  config:
    thresholdRatio: 0.8
    retainRatio: 0.16
    maxTokens: 8192
    compactionRetries: 1
