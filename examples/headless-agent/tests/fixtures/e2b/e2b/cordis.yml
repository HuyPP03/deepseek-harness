# One-world invariant (same pairing as examples/headless-agent/e2b.cordis.yml):
# e2b.cwd and sandbox-policy.workspaceRoot must name the same remote directory,
# which is also bash-local's implicit default workdir.
- id: e2b
  name: '@open-harness/oh-e2b'
  config:
    cwd: !!js process.cwd()
    timeoutMs: 180000

- id: subprocess-e2b
  name: '@open-harness/oh-subprocess-e2b'

- id: bash
  name: '@open-harness/oh-bash-local'
  config:
    timeoutMs: 30000

- id: fs-e2b
  name: '@open-harness/oh-fs-e2b'

- id: agents
  name: '@open-harness/oh-agent'

- id: sandbox-policy
  name: '@open-harness/oh-sandbox-policy'
  config:
    mode: danger-full-access
    workspaceRoot: !!js process.cwd()

- id: pty
  name: '@open-harness/oh-terminal'

- id: terminal-bash
  name: '@open-harness/oh-terminal-bash'
  config:
    pollIntervalMs: 25
    exactProbeAfterMs: 150
    idleSilenceMs: 2000
    handoffGraceMs: 500
    timeoutMs: 5000
    disposeGraceMs: 1000

- id: lsp
  name: '@open-harness/oh-lsp'

- id: lsp-stdio
  name: '@open-harness/oh-lsp-stdio'
  config:
    servers:
      fixture:
        command: node
        args:
          - !!js process.cwd() + '/fixture-lsp.mjs'
        extensionToLanguage:
          .ts: typescript
        shutdownTimeoutMs: 1000
        killGraceMs: 500
