# Add Code Mode and Cordis tools to the headless spawn/workflow stack.
- id: base
  name: '@deepseek-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: agent-spine
        name: '@open-harness/oh-agent-spine-demo'
        config:
          agents:
            - id: main
              provider: deepseek-official
              model: deepseek-v4-pro
              cwd: !!js process.cwd()
          workspaceContext:
            maxBytes: 65536
          tools:
            mode: both
          persona: |
            You are headless-agent, a coding assistant powered by the {{model}} model. Your working directory is {{cwd}}.

            Verify your work by running the code or tests. Keep answers brief and factual.
      - id: persistence
        name: '@open-harness/oh-session-persistence-jsonl'
        config:
          root: './.sessions'
          compression: !!js "process.env.OH_SNAPSHOT === undefined ? 'zstd' : 'none'"
      - insert:
          - id: code-runtime
            name: '@open-harness/oh-code-runtime-worker-thread'
          - id: cordis-host-runner
            name: '@open-harness/oh-cordis-host-runner'
          - id: tool-cordis
            name: '@open-harness/oh-tool-cordis'
