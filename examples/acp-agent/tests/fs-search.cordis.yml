# Minimal live counterpart for the glob-sampling snapshot composition.
- id: llm-deepseek
  name: '@open-harness/oh-llm-deepseek'
  config:
    models:
      - id: deepseek-v4-pro

- id: subprocess
  name: '@open-harness/oh-subprocess-local'

- id: bash
  name: '@open-harness/oh-bash-local'

- id: acp-agent
  name: '@open-harness/oh-acp-demo'
  config:
    provider: deepseek-official
    model: deepseek-v4-pro
    persistenceRoot: !!js process.env.OH_SNAPSHOT_SESSIONS_ROOT ?? './.sessions'
    persistenceCompression: !!js "process.env.OH_SNAPSHOT === undefined ? 'zstd' : 'none'"
    workspaceContext: false
    skills:
      enabled: false
    toolJobs: false
    goals: false
    persona: You are a concise snapshot agent working in {{cwd}}.

- id: tool-fs-search
  name: '@open-harness/oh-tool-fs-search'
  config:
    sampleOverCapGlobResults: true
    globMaxResults: 4
