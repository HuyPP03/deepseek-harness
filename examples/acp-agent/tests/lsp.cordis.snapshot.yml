# Keyless replay keeps the LSP composition intact and replaces only the model adapter.
- id: base
  name: '@open-harness/cordis-plugin-include'
  config:
    path: ../cordis.yml
    patches:
      - id: llm-deepseek
        name: '@open-harness/oh-llm-deepseek'
        disabled: true
      - insert:
          - id: lsp
            name: '@open-harness/oh-lsp'
          - id: lsp-stdio
            name: '@open-harness/oh-lsp-stdio'
            config:
              servers:
                fixture:
                  command: !!js process.execPath
                  args: ['./lsp-server.mjs']
                  extensionToLanguage:
                    '.ts': typescript
          - id: timeout-policy
            name: '@open-harness/oh-tool-call-timeout-policy'
          - id: tool-lsp
            name: '@open-harness/oh-tool-lsp'
            config:
              maxLocations: 1
          - id: llm-replay
            name: '@open-harness/oh-llm-replay'
