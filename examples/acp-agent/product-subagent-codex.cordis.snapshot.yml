# Keyless twin of product-subagent-codex.cordis.yml: keep the same product
# provider/tool composition and replace only the external model adapter.
- id: base
  name: '@deepseek-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: llm-deepseek
        name: '@open-harness/oh-llm-deepseek'
        disabled: true
      - insert:
          - id: llm-replay
            name: '@open-harness/oh-llm-replay'
            config:
              providers:
                - id: deepseek-official
                  name: DeepSeek
                  models:
                    - id: deepseek-v4-flash
                    - id: deepseek-v4-pro
          - id: subagent-codex
            name: '@open-harness/oh-subagent-codex'
          - id: tool-subagent-codex
            name: '@open-harness/oh-tool-subagent'
            config:
              provider: codex
              toolName: subagent_codex
              backgroundMode: one-shot
              maxDepth: provider-managed
