interface:
  display_name: "DSH Pre-Push Checks"
  short_description: "Run the relevant Open Harness checks before push"
  default_prompt: "Use $dsh-pre-push-checks before pushing this Open Harness branch."
